#include<main.h>
/**
 * _isupper - checks for uppercase character
 * @c: integer parameter
 * Return: 1 and 0 based on condition
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
return (0);
}
