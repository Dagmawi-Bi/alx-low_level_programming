#include <stdio.h>
/**
 * main -prints all single digit numbers of base 10 starting from 0
 * Return: value always 0
 */
int main(void)
{
int i;
for (i = 48; i <= 57; i++)
{
putchar(i);
}
putchar(10);
return (0);
}
