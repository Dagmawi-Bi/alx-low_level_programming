#include"main.h"
/**
 *  _islower -checks for lowercase character
 *  @c: character argument
 *  Return: value always 0 and 1 dependenig on condition
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
